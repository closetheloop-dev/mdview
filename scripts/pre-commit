#!/bin/sh
# Pre-commit quality gate: lint + tests. Linked from .git/hooks/pre-commit
# (see README "Development"); the global hooks delegate to it.
cd "$(git rev-parse --show-toplevel)" || exit 1
echo "pre-commit: bun run lint"
bun run lint || exit 1
echo "pre-commit: bun test"
bun test || exit 1
