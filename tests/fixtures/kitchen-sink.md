# Kitchen Sink

A fixture exercising **bold**, *italic*, ~~strikethrough~~, `inline code`,
and [a link](https://example.com) plus an autolink <https://auto.example.com>.

## Lists

- unordered item one
- item two with **bold** text that is long enough to need wrapping at narrow terminal widths for sure
  - nested item
    - deeper nested item
- item three

1. ordered first
2. ordered second
3. ordered third

- [x] completed task
- [ ] open task

## Blockquote

> A quote spanning multiple lines that should be wrapped and every wrapped
> line must carry the quote bar prefix.
>
> Second paragraph inside the quote.

## Code

```js
function hello(name) {
  return "hello " + name; // a somewhat long line of code that might get truncated in narrow terminals
}
```

## Table

| Tool | Language | Stars |
|:-----|:--------:|------:|
| glow | Go | 16000 |
| bat | Rust | 50000 |
| mdview | TypeScript | 1 |

## Unicode

CJK: 日本語のテキスト. Emoji: 👨‍👩‍👧 family. Accents: héllo wörld.

A very long unbreakable URL: https://example.com/some/extremely/long/path/that/cannot/possibly/fit/on/one/line/of/a/narrow/terminal/window/at/all

---

Final paragraph after a horizontal rule.
